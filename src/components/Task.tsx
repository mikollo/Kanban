import React, { useRef } from "react";
/*
function Task(props) {
  const titleInputElement = useRef(null);
  const descriptionInputElement = useRef(null);
  return (
    <div>
      <input ref={titleInputElement} type="text" name="Title" />
      <input ref={descriptionInputElement} type="text" name="Description" />
      <button
        onClick={() =>
          props.dispatch({
            type: "add task to list",
            indexOfList: props.indexOfList,
            newTaskData: {
              title: this.titleInputElement.value,
              description: this.descriptionInputElement.value
            }
          })
        }
      >
        Add
      </button>
    </div>
  );
}

export default Task;
*/
